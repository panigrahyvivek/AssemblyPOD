ID: mta_CustomAssemblyPod
_schema-version: '2.1'
parameters:
  deploy_mode: html5-repo
version: 0.0.1
modules:
  - name: mta_CustomAssemblyPod_appRouter
    type: approuter.nodejs
    path: mta_CustomAssemblyPod_appRouter
    parameters:
      disk-quota: 256M
      memory: 256M
    requires:
      - name: mta_CustomAssemblyPod_html5_repo_runtime
      - name: uaa_mta_CustomAssemblyPod
      - name: dest_mta_CustomAssemblyPod
  - name: mta_CustomAssemblyPod_ui_deployer
    type: com.sap.html5.application-content
    path: mta_CustomAssemblyPod_ui_deployer
    requires:
      - name: mta_CustomAssemblyPod_html5_repo_host
    build-parameters:
      requires:
        - name: CustomAssemblyPod
          artifacts:
            - './*'
          target-path: resources/CustomAssemblyPod
  - name: CustomAssemblyPod
    type: html5
    path: CustomAssemblyPod
    requires:
      - name: uaa_mta_CustomAssemblyPod
      - name: dest_mta_CustomAssemblyPod
    build-parameters:
      builder: grunt
      supported-platforms: []
      build-result: dist
resources:
  - name: mta_CustomAssemblyPod_html5_repo_runtime
    parameters:
      service-plan: app-runtime
      service: html5-apps-repo
    type: org.cloudfoundry.managed-service
  - name: mta_CustomAssemblyPod_html5_repo_host
    parameters:
      service-plan: app-host
      service: html5-apps-repo
    type: org.cloudfoundry.managed-service
  - name: uaa_mta_CustomAssemblyPod
    parameters:
      path: ./xs-security.json
      service-plan: application
      service: xsuaa
    type: org.cloudfoundry.managed-service
  - name: dest_mta_CustomAssemblyPod
    parameters:
      service-plan: lite
      service: destination
    type: org.cloudfoundry.managed-service
